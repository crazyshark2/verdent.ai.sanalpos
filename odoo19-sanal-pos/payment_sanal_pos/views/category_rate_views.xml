<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Kategori Bazlı Oran Tree View -->
    <record id="sanal_pos_category_rate_tree" model="ir.ui.view">
        <field name="name">sanal.pos.category.rate.tree</field>
        <field name="model">sanal.pos.category.rate</field>
        <field name="arch" type="xml">
            <tree editable="bottom">
                <field name="installment_id"/>
                <field name="category_id"/>
                <field name="interest_rate"/>
                <field name="is_active" widget="boolean_toggle"/>
            </tree>
        </field>
    </record>

    <!-- Kategori Bazlı Oran Search View -->
    <record id="sanal_pos_category_rate_search" model="ir.ui.view">
        <field name="name">sanal.pos.category.rate.search</field>
        <field name="model">sanal.pos.category.rate</field>
        <field name="arch" type="xml">
            <search>
                <field name="category_id"/>
                <field name="installment_id"/>
                <filter name="active" string="Aktif" domain="[('is_active','=',True)]"/>
                <group expand="0" string="Grupla">
                    <filter name="group_category" string="Kategori"
                            context="{'group_by':'category_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Kategori Bazlı Oran Action -->
    <record id="action_sanal_pos_category_rate" model="ir.actions.act_window">
        <field name="name">Kategori Bazlı Oranlar</field>
        <field name="res_model">sanal.pos.category.rate</field>
        <field name="view_mode">tree</field>
        <field name="context">{'search_default_active': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Kategori bazlı taksit oranı ekleyin
            </p>
            <p>
                Belirli ürün kategorileri için farklı faiz oranları tanımlayabilirsiniz.
            </p>
        </field>
    </record>

</odoo>
